name: Test CDK and App when pushing to main

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: .github/actions/test_cdk.yml
      - uses: .github/actions/test_app.yml
    timeout-minutes: 30