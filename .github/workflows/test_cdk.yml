name: Test CDK
on:
  push:
    branches-ignore:
      - main
    paths:
      - cdk/**
  workflow_call:
    branches:
      - main
    

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test CDK
        run: |
          echo "Test"
    timeout-minutes: 10
